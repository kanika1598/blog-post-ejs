<%- include('partials/header'); %>

    <h1>Home</h1>

    <p>
        <%= startingContent %>
    </p>

    <% newPosts.forEach(function(newPost){ %>

        <h4 style="color: #1abc9c;">
            <%= newPost.title %>
        </h4>
        <p>
            <%= newPost.content.substring(0,100) %>...<a href="/posts/<%= newPost._id %>">Read More</a>
        </p>
        <% }) %>

            <%- include('partials/footer'); %>
